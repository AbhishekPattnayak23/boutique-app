/** * User feedback handling module * @version 1.0.0 */(function() {'use strict';const feedbackElement = document.getElementById('feedbackMessage');if (!feedbackElement) {console.error('Feedback element not found in the DOM');return;}/** * Shows feedback message to the user * @param {string} message - Message to display * @param {boolean} isSuccess - Whether it's a success message * @param {number} [duration=null] - Auto-hide duration in ms, null for no auto-hide */function showFeedback(message, isSuccess, duration = null) {try {feedbackElement.className = 'feedback-message show';feedbackElement.classList.add(isSuccess ? 'feedback-success' : 'feedback-error');feedbackElement.textContent = message;const iconHTML = `<img src="${isSuccess ? 'success.svg' : 'error.svg'}"alt="${isSuccess ? 'Success' : 'Error'}"class="feedback-icon"width="16"height="16">`;feedbackElement.innerHTML = iconHTML + ' ' + message;if (duration && typeof duration === 'number') {setTimeout(() => {hideFeedback();}, duration);}} catch (error) {console.error('Error displaying feedback:', error);}}/** * Hides the feedback message */function hideFeedback() {try {feedbackElement.classList.remove('show');} catch (error) {console.error('Error hiding feedback:', error);}}feedbackElement.addEventListener('click', hideFeedback);window.feedbackSystem = {showFeedback,hideFeedback};if (typeof module !== 'undefined' && module.exports) {module.exports = {showFeedback,hideFeedback};}})();