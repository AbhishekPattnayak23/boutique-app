{
  "name": "responsive-login-page",
  "version": "1.0.0",
  "description": "Responsive login page implementation",
  "main": "auth.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "echo \"Starting application\" && exit 0"
  },
  "dependencies": {
    "bootstrap": "^5.3.2"
  }
}
